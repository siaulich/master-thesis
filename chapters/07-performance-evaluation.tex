\chapter{Evaluation of the ML-Based Reconstruction}
\label{chap:performance-evaluation}
To evaluate the performance of the ML-based reconstruction method, several metrics and analyses are conducted. This chapter presents the results of these evaluations, focusing on the accuracy of jet assignments, the reconstruction of relevant physics variables, and performance across different kinematic regions. The ML-based methods are compared against the baseline reconstruction techniques, both overall and in specific regions of interest, such as near the $t\bar{t}$ production threshold.

\section{Assignment Accuracy}
\label{sec:evaluation:assignment}
\begin{table}[ht]
    \centering
    \caption{Comparison of assignment and selection accuracies for         different reconstruction methods. The uncertainties represent the     statistical variations in the evaluation dataset.}
    \input{plots/compact_assigner/accuracy/reconstruction_accuracies_table}
    \label{tab:reconstruction-accuracies}
\end{table}
The assignment accuracy is defined as the fraction of events in which reconstructed jets are correctly assigned to their parent partons. The selection accuracy, on the other hand, measures the fraction of events where the correct jets are among the selected candidates. Both metrics are crucial for evaluating reconstruction methods, as they directly impact the resolution of physics observables and the sensitivity of analyses. Furthermore, some variables, such as the invariant mass of the $t\bar{t}$ system, are sensitive only to the correct selection of candidates, making selection accuracy particularly important for certain analyses. \Cref{tab:reconstruction-accuracies} summarises the overall assignment performance of the reconstruction methods.

The ML-based method, represented by the CompactTransformer, shows a significant improvement in assignment accuracy over the $\Delta R$ and $\chi^2$ methods. Concerning the baseline methods, the $\chi^2$ method performs roughly $16\%$ more accurately than the $\Delta R$ method. For the selection accuracy, both baseline methods yield the same result within uncertainties, as they use the same jet selection criteria. The improvement in the CPT's selection accuracy is rather small, which can be attributed to $b$-tagging. Because baseline methods almost exclusively rely on $b$-tagging for selection, and the GN2-tagger is very performant~\cite{gn2_btagger}, the selection accuracy is already very high. Furthermore, the $b$-tagging of the GN2-tagger already accounts for the full kinematic information of the event, making it difficult for the ML-based method to significantly improve selection accuracy beyond that of the $b$-tagger. This is also supported by the model's reliance on the input features, as shown in \cref{sec:assignment:feature_importance}. The investigation revealed that the selection accuracy mainly depends on the $b$-tag. It does, however, utilise information from the kinematics of the jets and leptons, giving it an edge over baseline methods that rely solely on $b$-tagging and jet $p_T$. Regarding assignment accuracy, the ML-based method shows a significant improvement, attributable to its ability to learn complex correlations among the kinematic variables of the reconstructed particles. These correlations can be much more efficient than the simple criteria used by baseline methods, allowing them to surpass those methods' performance.
\begin{figure}[ht]
    \centering
    \includegraphics[width=0.7\textwidth]{plots/compact_assigner/binned_performance/N_jets/binned_accuracies_N_jets.pdf}
    \caption{Assignment accuracy as a function of the number of jets in the event for different reconstruction methods. The background histogram shows the distribution of events as a function of the number of jets. The uncertainties represent the statistical variations in the evaluation dataset.}
    \label{fig:accuracy_vs_njets}
\end{figure}%

\Cref{fig:accuracy_vs_njets} shows the assignment accuracy for events with different jet multiplicities for each method. The CPT outperforms the baseline methods across all bins. For all methods, performance drops as the number of jets increases. The drop, however, is relatively small compared to the number of permutations, which increases quadratically. While this can be attributed to the $b$-tagging performance, it contrasts sharply with the methods for the all-hadronic channel, where performance drops sharply for events with many jets in the final state, as seen in reference~\cite{spanet}. This is, of course, a consequence of the much lower number of possible permutations for this decay channel. Even with additional jets, the chance of a jet being falsely $b$-tagged is quite low~\cite{gn2_btagger}. Thus, the performance of selecting the correct jets degrades much less than in the hadronic channels, where non-tagged jets must also be selected and assigned to the $W$ bosons' decay products.
Notably, the CPT shows the smallest performance drop, and the gap widens as the number of jets increases.

This stems from improved the selection accuracy, as shown previously. In \cref{sec:assignment:plots}, \cref{fig:selection_accuracy_vs_n_jets} shows the selection accuracy binned in the same way. The CPT does a much better job at selecting the correct jets for events with higher jet multiplicity. It was found that this is the primary cause for differences in assignment accuracy with respect to jet multiplicity. The efficiency of assigning correctly selected jets appears to be largely independent of jet multiplicity.
\begin{figure}[ht]
    \centering
    \includegraphics[width=0.7\textwidth]{plots/compact_assigner/binned_performance/truth_ttbar_mass/binned_accuracies_truth_ttbar_mass.pdf}
    \caption{This plot shows the assignment accuracy in bins of the true invariant mass of the top-quark pair. The background histogram shows the distribution of events as a function of the number of jets. The uncertainties represent the statistical variations in the evaluation dataset.}
    \label{fig:accuracy_vs_ttbar_mass}
\end{figure}%

\Cref{fig:accuracy_vs_ttbar_mass} shows the assignment accuracy as a function of the parton-level \mttbar. All methods show similar behaviour: the accuracy drops at low invariant masses and appears to saturate at high invariant masses. For the CPT, the high-invariant-mass regime approaches unity, indicating near-perfect reconstruction. At the other end of the spectrum, the performance drops to around $0.6$ near the \ttbar production threshold. In this region, the CPT and the $\chi^2$ method appear to be almost on par. Overall, the $\chi^2$ method is the least sensitive to \mttbar, while the $\Delta R$ method is the most sensitive.

As found earlier, the number of jets can affect the accuracy. The double-binned distribution of events with respect to the invariant mass \mttbar and number of jets in \cref{fig:feature_2d_ttbar_mass_njets} shows that lower-energy events have fewer jets on average. Naively, this should increase assignment accuracy, but the opposite is observed.

To decouple the number of jets per event from the selection accuracy, one can consider the conditional probability of making a correct assignment, given that the correct jets were selected. In this case, the assignment reduces to a binary problem in the dileptonic decay, as jets can only be assigned to bottom or antibottom quarks. Thus, the probability of a correct assignment at random is $50\%$.
According to Bayes' theorem, the conditional probability for each method can be computed by dividing its assignment accuracy by its selection accuracy.
\begin{figure}[ht]
    \centering
    \includegraphics[width=0.7\textwidth]{plots/compact_assigner/binned_performance/truth_ttbar_mass/binned_accuracy_quotients_truth_ttbar_mass.pdf}
    \caption{Quotient of assignment and selection accuracy computed in     bins of the parton-level \mttbar. The background histogram shows the distribution of events as a function of the number of jets. The uncertainties represent the statistical variations in the evaluation dataset.}
    \label{fig:accuracy_quotient_vs_ttbar_mass}
\end{figure}
The corresponding plot showing this probability for the different methods, binned in parton-level \mttbar, is displayed in \cref{fig:accuracy_quotient_vs_ttbar_mass}. One thing to note is that the $\Delta R$ method has a performance below $0.5$ for the lowest bin, meaning that it performs worse than a random assignment in this bin. This suggests that the particle topology in this region disfavours assigning jets based on proximity. \ref{fig:deltaR_pairing} in \cref{chap:add-plots} clearly shows the difference in angular distance distributions for correct and incorrect jet-lepton pairing between the inclusive sample and the near-threshold region. This explains the significant performance drop-off in this region.
Going further, this behaviour can be directly linked to the underlying physics: at the production threshold, the top quarks carry very little energy.
Thus, the decay products of the top quarks, i.e., the jets, the $W$-boson, and subsequently the lepton, are less collimated. The bottom quark and $W$-boson are back-to-back in the top quark's rest frame and become more collimated through the Lorentz boost of the top quark with respect to the lab frame. The strength of this boost is directly proportional to the top quark's energy, which is approximately proportional to \mttbar. Note that this picture becomes more complicated when the chiral nature of the decays is taken into account, as it can affect the angular distributions.
As the energy increases, decay objects become increasingly collimated. While this explains why performance drops, it does not explain how accuracy can be worse than with random jet assignment. To explain this \ more thoroughly, investigations into the angular structure of the events in this region would be necessary, which is outside the scope of this thesis.
Another observation is that in the lowest bin, the CPT and $\chi^2$ method are completely on par for this metric. Performance losses of the $\chi^2$ method can be explained by the \cref{fig:invariant_mass_pairing} in \cref{chap:add-plots}, which shows the different distributions of the reconstructed top-mass for the correct and incorrect assignment of the jets. For comparison, the distributions for the near-threshold and inclusive samples are shown. The correct pairing shows the typical Breit-Wigner mass peak, as expected, whereas the incorrect pairs show a washed-out distribution. For the low-energy events, the difference between the two distributions is smaller but still very pronounced. One reason is that the top quarks tend to be more off-shell, and faulty assignments do not change the kinematics as dramatically. Generally, however, the assignment based on the invariant mass seems relatively robust.
For machine-learning methods, it is difficult to assess which specific features lead to performance degradation. The leading feature, i.e. the $b$-tag, is not found to have a varying performance across the invariant mass \mttbar—as seen in \cref{fig:b_tag_performance_ttbar_mass}. For the other features, assessing their effect across different regimes is non-trivial.
One way is to examine whether certain features correlated with near-threshold events affect performance.
A particular example for this is the angular separation $\Delta R(\ell_1,\ell_2)$ of the leptons. The property of having less energy, and thus less boosted top-quarks, also significantly impacts the angular separation of the leptons themselves. While decays of high-energy top-quark pairs lead to back-to-back leptons, the opposite is true for low-energy top-quark pairs.
The double-binned distribution of events in \cref{fig:feature_2d_ttbar_mass_dR_l1l2} in \cref{chap:add-plots} shows that events with lower \mttbar tend to have more collimated leptons, whereas they are more separated for more energetic events.
Since the angular separation of the leptons is suspected to degrade the accuracy, its impact on the individual methods is investigated.
\begin{figure}[ht]
    \centering
    \includegraphics[width=0.7\textwidth]{plots/compact_assigner/binned_performance/dR_l1l2/binned_accuracies_dR_l1l2.pdf}
    \caption{Assignment accuracy binned in the angular distance between the two leptons of the events $\Delta R(\ell_1,\ell_2)$. The background histogram shows the distribution of events as a function of the number of jets. The uncertainties represent the statistical variations in the evaluation dataset.}
    \label{fig:accuracy_vs_dr_l1l2}
\end{figure}
As before, \cref{fig:accuracy_vs_dr_l1l2} shows the assignment accuracy in bins of the angular distance $\Delta R(\ell_1,\ell_2)$
between the two leptons. All methods suffer performance losses for events with close leptons. Interestingly, however, for the smallest\footnote{Due to the overlap-removal, which removes overlapping objects from the (simulated) events, there is a cut-off at $\Delta R<0.3$. This technique accounts for the fact that in a detector, objects that coincide cannot be separated.} angular separation, the performance is still above that of near-threshold events. This implies that the decreased performance due to the reduced angular separation in the near-threshold region is one contributing factor, but not the only one.
A combined comparison of both variables' impact is shown in \cref{fig:accuracy_2d_ttbar_mass_dR_l1l2} in \cref{chap:add-plots}. This distribution provides a more comprehensive picture by separating the effects of both variables. Also, it allows us to isolate the effects of each by varying one variable while holding the other constant. The double-binned accuracy reveals that the invariant mass \mttbar has a much stronger impact on performance than the angular separation alone. Expectedly, the $\Delta R$ Method is the most sensitive to the angular separation.
For the lowest bins in $\Delta R(\ell_1,\ell_2)$, the performance breaks down significantly and is nearly constant for varied \mttbar.
The other methods' performances reduce much more in the rows of varied \mttbar, than they do in the columns of varied $\Delta R(\ell_1,\ell_2)$. This observation holds for all bins of energies and angular separations considered here.
From this, it follows that the near-threshold region poses special challenges for jet assignment. For the traditional methods, explanations are quite clear, but even the transformer, which can leverage very complex correlations between variables, performs only slightly better than the baseline methods.
While some impact factors could be identified, i.e. angular separation of the leptons and off-shell tops, the complete picture of why this region is particularly challenging remains elusive. Due to increased efforts to study quantum effects or quasi-bound-state effects at the production threshold, this remains a highly relevant question for future investigations.
While it was found that other kinematic variables have regimes with quite different performance on assignment accuracy, none had an impact as severe as that of the invariant mass \mttbar.

The training did not include quasi-bound-state effects, which primarily contribute at low invariant masses. To investigate the effects of this topology, a retraining of the models was run to compare models are trained with the nominal sample, i.e. $t\bar{t}_{\text{pQCD}}$, (\textit{Nominal}), a sample modelling the quasi-bound-state effects, i.e. $t\bar{t}_{\text{NRQCD}}$, (\textit{Toponium}), and the nominal sample with a selection cut required $m(t\bar{t})<450~\GeV$ (\textit{Threshold}), respectively.

It was found that a model, despite being trained only with events at threshold, performs poorly at reconstructing events from the $t\bar{t}_{\text{NRQCD}}$ sample. This highlights that reconstructing the $t\bar{t}_{\text{NRQCD}}$ events poses a significant challenge, distinct from the loss of invariant masses alone. The corresponding figure and table can be found in \cref{sec:region_training}.

The observed behaviour is expected to result from the distinct spin structure, which induces different angular correlations. A thorough investigation of these effects is, however, beyond the scope of this work.

\section{Neutrino Regression}
\label{sec:evaluation:regression}
This section analyses and compares the neutrino regression of the MMT against existing baseline methods.
Further, the impact of different loss functions for the MMT is investigated. For this study, only the regression models are considered. The model trained with the Gaussian-loss showed the same performance as the MMT with the Cartesian-loss when using the mean. However, when using random sampling, a consistent degradation in performance was observed, as expected, since it is equivalent to adding Gaussian noise to the reconstructed value. \Cref{chap:add-plots} provides a summary of this in the \cref{tab:gaussian_loss:mae:cartesian,tab:gaussian_loss:mae:PtEtaPhiE}.
Due to the $\nu$-Weighter's non-unity efficiency of about $\epsilon=94.0\%$, the evaluation of each method is done on the subset of events, where the $\nu$-Weighter is available. Note that this can introduce biases in the evaluation.
Various metrics are used to assess the method's quality. It should also be noted that evaluating the quality of the regression is much more nuanced compared to the assignment. As demonstrated below, ranking the performance of one method against another depends heavily on the metric one employs. This is because the reconstructed object is essentially six-dimensional, and its deviation must be projected in some way. Even quantifying a one-dimensional error comes with some degeneracy; different powers, for example, might put emphasis on larger or smaller deviations. Also, whether a relative or absolute deviation is considered can make a huge difference.
To account for this, different metrics are used and compared.
\begin{table}[ht]
    \centering
    \caption{Summary of the  MAE of the different neutrino regression methods for the Cartesian coordinates of the neutrinos in the lab frame. For the MMT, the loss function used during training is shown in brackets. The uncertainties represent the statistical variation in the dataset.}
    \input{plots/compare_losses_nu_weighter/neutrino_deviations/regression_neutrino_mae.tex}
    \label{tab:mae:cartesian}
\end{table}

\Cref{tab:mae:cartesian} shows the MAE of the neutrino momentum components for the different methods.
Each MMT model, regardless of the loss function, achieves a lower MAE for each of the three components than the two baseline methods do. Some impact of the loss functions used here can be identified. Notably, the Cartesian-Loss provides the lowest MAE for the transverse components.
However, the performance is nearly on par with that of the model trained with the PtEtaPhi-loss. Interestingly, the model trained with the MagDir-loss achieves the smallest error for the longitudinal component. Even though this might seem counterintuitive, it can be attributed to the model with Cartesian loss being trained on a squared error, which might disfavour larger errors more strongly than an absolute error would. This already highlights that very subtle changes in evaluation metrics might lead to different conclusions about the model's performance.
Another noteworthy observation is that the difference between $\nu^2$-Flows and $\nu$-Weighter is much larger than the difference between the former and the MMT-architecture. As the only regression method that does not rely on machine learning, the $\nu$-Weighter is found to perform significantly worse than all ML-based methods. The MMT is found to reduce the MAE by a factor of $2$, a significant improvement.
This becomes even more impressive when one considers the lower computational cost of ML-based methods.
Another trivial remark is that all methods show the same performance for the two transverse components $x$ and $y$, as expected, since the detector and physics are rotation-invariant with respect to the beam direction.
\begin{table}[ht]
    \centering
    \caption{Summary of the MAE of the neutrinos'
        $p_T$, $\eta$, $\phi$, and $E$, evaluated for the different methods. Note that the computation of the error in $\phi$ was     performed periodically.}
    \input{plots/compare_losses_nu_weighter/neutrino_deviations/regression_neutrino_PtEtaPhiE_mae.tex}
    \label{tab:mae:PtEtaPhiE}
\end{table}%

To highlight the performance variance between different metrics, \cref{tab:mae:PtEtaPhiE} shows the MAE for the $p_T$, $\eta$, $\phi$, and $E$ of the neutrinos.
As for the neutrino momentum components, models based on the MMT, trained with different loss functions, outperform both baseline methods across all metrics. Further, for this set of variables, the $\nu$-Weighter has the largest errors across all variables. However, for the pseudorapidity, it is on par with $\nu^2$-Flows. For the differently trained models, variations in the performance can be found among all variables. The PtEtaPhi-loss provides the smallest error among $p_T$, $\eta$, and $\phi$, which is, of course, consistent with those being its target for optimisation.
Similarly, the model trained with the MagDir-loss has the smallest error in neutrino energy, which is equivalent to their magnitude since they are massless. Both the PtEtaPhi- and MagDir-loss reduce the error in $\eta$ by about $20\%$ with respect to the Cartesian-loss. To sum up, this reveals a strong dependence on the choice of loss function, despite using the same architecture. As different loss functions can reduce errors for some variables while increasing them for others, choosing the most appropriate loss function depends on the variable of interest in the analysis.

To avoid showing each loss function for every one of the following analyses, the main loss function considered here is the Cartesian loss. Unless stated otherwise, the MMT is always trained with the Cartesian loss.
\begin{figure}[ht]
    \centering
    \includegraphics[width=0.7\textwidth]{plots/compact_reconstructor_nu_weighter/binned_performance/truth_ttbar_mass/binned_dev_angle_truth_ttbar_mass.pdf}
    \caption{Mean angle between the true and predicted neutrino vectors binned with respect to the parton-level invariant mass of the top-quark pair system. The background histogram shows the distribution of events as a function of the number of jets. The uncertainties represent the statistical variations in the evaluation dataset.}
    \label{fig:dev_angle_ttbar_mass}
\end{figure}%
\Cref{fig:dev_angle_ttbar_mass} shows the angle between the true and predicted vectors of the neutrinos. This metric was chosen for comparisons across different energy regimes because it is scale-free and independent of the neutrino masses involved. As the energy of the neutrinos involved is strongly correlated with the invariant mass of the \ttbar system, choosing a solid metric for this comparison was particularly difficult. The first observation is that the MMT consistently outperforms the other methods in providing, on average, a smaller directional deviation of the predicted neutrinos.
Notably, this difference is substantial across the entire spectrum and much larger than the difference between the two baseline methods. Another notable feature is that all methods show a nearly linear dependence on the invariant mass \mttbar as the angular distance increases at lower masses. While this trend appears to be very similar across all methods, the slope of the $\nu$-Weighter seems to increase further in the lowest-mass bins, leading to a growing difference between the two baseline methods. One major impact factor for this behaviour is the decay angle $\theta(\ell,\nu)$, which is defined as 
\begin{align}
    \theta(\ell,\nu)=\arccos\left(\hat \ell\cdot \hat \nu\right)~,
\end{align}

where $\hat\ell$ and $\hat \nu$ represent the normalised three-momentum vector of the lepton and neutrino, respectively.

\Cref{fig:feature_2d_ttbar_mass_angle_nu_lep} in \cref{chap:add-plots} shows the double-binned distribution of neutrinos with respect to the event's invariant mass \mttbar and the angle between the lepton and the neutrino at parton-level. It is clear that the distribution of angles between the lepton and the neutrino is broader and peaks at a higher value in the low invariant-mass regime. This can, as before, be attributed to the lower energy of top quarks and $W$ bosons in this region, which leads to decreased collimation of W-boson decay products.
\begin{figure}[ht]
    \centering
    \includegraphics[width=0.7\textwidth]{plots/compact_reconstructor_nu_weighter/binned_performance/truth_lepton_nu_angle_mean/binned_dev_angle_truth_lepton_nu_angle_mean.pdf}
    \caption{Mean angle between the true and predicted neutrino vectors binned with respect to the parton-level angle $\theta(\ell,\nu)$
    between lepton and neutrino. The background histogram shows the distribution of events as a function of the number of jets. The uncertainties represent the statistical variations in the evaluation dataset.}
    \label{fig:dev_angle_angle_nu_lep}
\end{figure}%

To investigate the methods' dependences on the angle between lepton and neutrino, \cref{fig:dev_angle_angle_nu_lep} shows the binned evaluation of the angular discrepancy. All methods show a significant increase in the angle between truth and reconstruction as the angle between the lepton and neutrino increases. The range covered by this variation is far larger than the variation seen for different invariant masses \mttbar.
It can further be noted that the MMT appears to be most sensitive to the angle $\theta(\ell,\nu)$, since it exhibits the largest difference in performance between the largest and smallest bin of the variable. While for the bins corresponding to collinear leptons and neutrinos, the MMT can provide the best performance in terms of angular deviation between truth and prediction, $\nu^2$-Flows provides the best performance for events for large angle $\theta(\ell,\nu)$.

\Cref{fig:dev_angle_ttbar_mass_angle_nu_lep} in \cref{chap:add-plots} provides a combined investigation of the impact of both variables. The double-binned evaluation of the angle between true and prediction shows that the impact of the angle between lepton and neutrino dominates significantly compared to the invariant mass of the top-quark pair system. All methods, but the $\nu$-Weighter in particular, seem to be largely independent of the \mttbar for fixed bins of $\theta(\ell,\nu)$. Thus, the difference observed in \cref{fig:dev_angle_ttbar_mass} seems to be largely explained by the correlated variable $\theta(\ell,\nu)$.

The impact of $\theta(\ell,\nu)$ on the magnitude and scaling of neutrinos was found to follow a very different pattern. As this is only tangibly interesting for the following analyses, a discussion is omitted here, and the corresponding plot can be found in \cref{fig:dev_magnitude_angle_nu_lep} in \cref{chap:add-plots}.

No significant sensitivities were identified for other event variables examined in this thesis.

\section{Variable Reconstruction}
\label{sec:evaluation:variable-reconstruction}
The primary interest in providing jet assignment and neutrino regression lies in the subsequent reconstruction of event variables.
Therefore, studying the impact of the proposed methods on the quality of event reconstruction is critical. To evaluate this, various variables of interest commonly used in analysis settings are examined. The variables investigated are primarily the invariant mass \mttbar and the spin-sensitive markers \chel and \chan. As these were the main variables used in recent \atlas analyses. Since the neutrino reconstruction of the $\nu$-Weighter was found to offer the poorest resolution here, it was dropped to evaluate the variable reconstruction. Further, it is the only method that yields non-unity efficiencies; thus, the other variables can be evaluated without any subsampling, allowing a less biased evaluation.

The reconstruction performance is mostly evaluated in terms of the 
\begin{align}
    \text{Mean Deviation} &=\mu\left(x_{\text{reco}} - x_{\text{true}}\right)~,\\
    \text{Mean Relative Deviation} &=\mu\left(\frac{x_{\text{reco}} -
    x_{\text{true}}}{x_{\text{true}}}\right)~,
\end{align}
and 
\begin{align}
    \text{Resolution}&=\sigma\left(x_{\text{reco}} - x_{\text{true}}\right)~,\\
    \text{Relative Resolution}&=\sigma\left(\frac{x_{\text{reco}} -
    x_{\text{true}}}{x_{\text{true}}}\right)~,
\end{align}
where $x_{\text{reco}}$ and $x_{\text{true}}$ denote the reconstructed and true value of the variable; the operators $\mu$ and $\sigma$ are the mean and standard deviation computed over the sample of events, respectively.
Unless stated otherwise, the true value of a variable is always computed at the parton-level and after final-state radiation.
While the mean (relative) deviation is a measure of any biases and skew in the reconstructed distributions, the (relative) resolution is a measure of the spread of the reconstructed variable around the true value. Both of these are crucial to assess the quality of the reconstruction. For fits, however, resolution tends to be more important, as non-zero deviations and skews are corrected by the fit — provided they are consistent in both data and simulation. A distinction is made between the difference attributed to the jet assignment and the neutrino regression. Therefore, improvements due to increased assignment performance are studied first.

\subsection{Impact of Novel Assignment Methods}
First, the effect of the improved jet assignment is examined in isolation. To isolate the assignment's impact, all methods are evaluated interfaced with $\nu^2$-Flows. The first variable to be investigated is the invariant mass \mttbar.
\begin{table}[ht]
    \centering
    \caption{Summaries of the reconstruction metrics for invariant mass \subrefb{tab:assignment:mttbar} and spin-sensitive variables \subrefb{tab:assignment:c_hel} \& \subrefb{tab:assignment:c_han} for the different jet assignments methods in conjunction with $\nu^2$-Flows. The uncertainties represent the statistical variations in the evaluation dataset.}
    \begin{subtable}{1.0\textwidth}
        \centering
        \caption{\textbf{Invariant Mass} \mttbar}
        \label{tab:assignment:mttbar}
        \input{plots/true_assignment/deviations/ttbar_mass_reconstruction_metrics_table.tex}
    \end{subtable}
    \begin{subtable}{1.0\textwidth}
        \centering
        \caption{\textbf{Spin-Sensitive Variable} \chel}
        \label{tab:assignment:c_hel}
        \input{plots/true_assignment/deviations/c_hel_reconstruction_metrics_table.tex}
    \end{subtable}
    \begin{subtable}{1.0\textwidth}
    \centering
        \caption{\textbf{Spin-Sensitive Variable} \chan}
        \label{tab:assignment:c_han}
        \input{plots/true_assignment/deviations/c_han_reconstruction_metrics_table.tex}
    \end{subtable}
\end{table}%

\Cref{tab:assignment:mttbar} summarises the relative deviation and relative resolution of the invariant mass \mttbar.
Note that the invariant mass is only sensitive to the selection of the jets, and not their assignment, as the computation is permutation invariant to the jet-ordering. As expected, both baseline methods achieve the same performance within the statistical uncertainties.
While the mean relative deviation is positive and close to zero for the baseline methods, the CPT yields a much larger negative mean relative deviation. This means that the reconstruction with the CPT is biased towards smaller invariant masses. For the resolution, on the other hand, one can see that the CPT achieves a resolution, smaller by around $17\%$. Comparing with the values obtained by using the truth-matched assignment, it clearly shows that the bias in the reconstructed \mttbar induced by the CPT's assignment is very close to the one with the true assignment of the jets. Thus, the bias can be assumed to be an effect from other parts of the reconstruction, which is cancelled out by the other methods, rather than a shortcoming of the transformer. Furthermore, the true assignment does not show an improvement exceeding the statistical uncertainties. While this is hardly surprising, given the transformer's selection accuracy of about $96.6\%$, it implies that no measurable improvement in the resolution of the \mttbar is expected from further optimisation of the jet selection.

The mean deviation and resolution for the two spin-sensitive markers \chel and \chan are summarised in \cref{tab:assignment:c_hel,tab:assignment:c_han},
respectively. As before, the $\chi^2$ method attains the lowest mean deviation for both variables, indicating that it again provides the least biased reconstruction. Previously, it provided the same unbiased reconstruction; however, for this variable, the $\Delta R$ method exhibits the highest bias, with the mean deviation for the \chan being roughly four times that of the next-largest method, which notably is the combination of true assignment with $\nu^2$-Flows. As this variable largely depends on the angular relation between the leptons and the jets, it is expected to be substantially more biased for a Method that enforces the proximity of jets and leptons. The CPT shows significantly larger biases than the $\chi^2$ method for both variables. However, the same is true for the reconstruction with the true assignment interfaced with $\nu^2$-Flows. As before, this implies that inherent biases in other aspects of reconstruction (such as $\nu^2$-Flows itself) give rise to parton-level deviations that are cancelled by the $\chi^2$ method.

Regarding the resolution, it is worth noting that it is generally much poorer than that of the invariant mass. Furthermore, both variables show the same trend across methods, with the CPT achieving the highest resolution. The $\chi^2$ method comes in second, and its difference in resolution is about the same towards both methods for the two variables. While no significant improvement in resolution was observed for the reconstruction of the \mttbar with the true assignment, this is substantially different for these variables. This highlights their strong dependence on the assignment in contrast to the invariant mass. For both variables, the difference between the perfect assignment and CPT is larger than its advantage over the $\chi^2$ method. Thus, for these variables, significant increases are expected as jet assignment improves, motivating future research in this area.


As before, the resolution of the reconstructed variables was examined across different event variables. Once again, the most interesting variable was the invariant mass of the \ttbar system.
For the resolution of the reconstructed \mttbar, \cref{fig:assignment:ttbar_mass_binned_reso_ttbar_mass} clearly shows that, across all methods, resolution degrades at low invariant mass. Notably, this increase is much more pronounced for the baseline methods, despite the selection accuracy being constant across the invariant mass spectrum for all methods. So this difference cannot be explained by a simple dependence on the jet selection, but reflects systematic effects in the misassignments across the different methods. As before, the resolution achieved by the CPT is on par with the perfect assignment across the full spectrum of invariant masses considered here. Thus, the drop-off in performance for the CPT can be attributed to other aspects of the reconstruction. Most notable is the error of the neutrino-regression as provided by $\nu^2$-Flows, which was shown in \cref{sec:evaluation:regression} to be larger for lower invariant masses.
For the resolution of the second variable, \chel, a different picture emerges from \cref{fig:assignment:ttbar_mass_binned_reso_c_hel}. The resolution achieved by the perfect assignment shows a steady improvement towards lower masses, indicating that the error of the neutrino regression is less important in this region. This finding already contrasts with the observation for the invariant mass \mttbar. For the assignment methods investigated here, the resolution is most stable at higher invariant masses; it shows the worst performance across all methods at the bulk of the distribution around $m(t\bar{t})\sim 450$ GeV. Towards the lowest mass bin, all methods show again an increase in performance, which is most pronounced for the $\chi^2$ method. In this single bin, it outperforms the resolution achieved by the CPT. This improvement in resolution is inconsistent with the steadily decreasing assignment accuracy shown in \cref{fig:accuracy_vs_ttbar_mass}. Therefore, the effect seems to stem from a combination of the neutrino regression and the reduced assignment accuracy. However, the significantly larger improvement of the $\chi^2$ method remains elusive. This underscores that not only the quantity of misassignments of the different methods but also their structure is decisive for the resolution of a variable.

\begin{figure}[H]
    \centering
    \includegraphics[width=0.7\textwidth]{plots/true_assignment/binned_performance/truth_ttbar_mass/binned_resolution_ttbar_mass_truth_ttbar_mass.pdf}
    \caption{Relative Resolution of \mttbar computed in bins of the parton-level invariant mass of the \ttbar system. The different methods, as well as the expected resolution with perfect efficiency for the assignment, are compared. The background histogram shows the distribution of events as a function of the number of jets. The uncertainties represent the statistical variations in the evaluation dataset.}
    \label{fig:assignment:ttbar_mass_binned_reso_ttbar_mass}
\end{figure}

\begin{figure}[H]
    \centering
    \includegraphics[width=0.7\textwidth]{plots/true_assignment/binned_performance/truth_ttbar_mass/binned_resolution_c_hel_truth_ttbar_mass.pdf}
    \caption{Resolution of \chel}
    \caption{Resolution of \chel computed in bins of the parton-level invariant mass of the \ttbar system. The different methods, as well as the expected resolution with perfect efficiency for the assignment, are compared. The background histogram shows the distribution of events as a function of the number of jets. The uncertainties represent the statistical variations in the evaluation dataset.}
    \label{fig:assignment:ttbar_mass_binned_reso_c_hel}
\end{figure}

\subsection{Impact of Novel Neutrino Regression}
\begin{figure}[H]
    \centering
    \includegraphics[width=.7\textwidth]{plots/compact_reconstructor/deviations/ttbar_mass_deviation_distributions_combined.pdf}
    \caption{Relative deviation of the invariant mass of the         \ttbar system, \mttbar, compared to the parton-level. The legend shows the mean relative deviation and the relative resolution for all methods.}
    \label{fig:compact_reconstructor:ttbar_mass_deviation}
\end{figure}

To keep the investigated combinations of analysis setups feasible, the $\Delta R$ method is omitted from further investigations.
\Cref{fig:compact_reconstructor:ttbar_mass_deviation} shows the relative deviation of the invariant mass as before, including the reconstruction using the MMT. The first observation is that the MMT deviations show a very large negative shift—meaning the reconstructed mass is smaller than the true mass. This is also evident in the mean relative deviation for the MMT, which is substantially more negative at about $-0.133$, compared to the CPT with $\nu^2$-Flows at $-0.02$. For the relative resolution, on the other hand, a clear improvement over the reconstruction with $\nu^2$-Flows is evident. With a reduction of about $40\%$ compared to the CPT with $\nu^2$-Flows, this yields an improvement by a factor of $2$ over the baseline $\chi^2$ method.

To examine the biases' impact on the shape of the distributions of the invariant mass, \cref{fig:losses:ttbar_mass_distribution} compares the distributions at parton- and reconstruction-level for the different methods. The figure compares the different losses used with the CPT interfaced with $\nu^2$-Flows. The last reconstruction setup produces a reasonable distribution at the reco-level that matches the parton-level features, with the most notable discrepancy in the low-mass region. Two of the losses investigated, Cartesian- and PtEtaPhi-loss, show strong deviations in the shape of the distribution, which is also in good agreement with the observation of the bias for the relative deviation. Both reconstructions show a significant shift in the distribution's peak. Further, the bins below the production threshold of $m(t\bar{t})\sim 2\cdot m_t$ are strongly populated.
The distribution of the model trained with the MagDir-loss shows a closer resemblance to the distribution, but displays stronger deviations in the shape compared to the reconstruction based on $\nu^2$-Flows. \Cref{tab:losses:ttbar_mass_reconstruction} summarises the overall performance of the different tools for the invariant mass \mttbar. It shows that the MagDir-loss achieves the best resolution among all methods and has the lowest bias among all MMT-models, which is about a factor of $2$ smaller than that of the other models. Thus, it is consistent with the distributions in \cref{fig:losses:ttbar_mass_distribution}.
\begin{figure}[ht]
    \centering
    \includegraphics[width=1.\textwidth]{plots/compare_losses/distributions/ttbar_mass_distributions.pdf}
    \caption{Comparison of distributions of reconstructed and true         \mttbar for different reconstruction methods. Note that true refers to the parton-level.}
    \label{fig:losses:ttbar_mass_distribution}
\end{figure}


\Cref{tab:losses:chel_reco,tab:losses:chan_reco} summarise the comparison of the different MMT models with $\nu^2$-Flows for \chel and \chan, respectively, by showing mean deviation and resolution. Despite small differences in the mean deviation and resolution of the variables, the overall pattern remains the same: the MMT-based reconstruction provides the lowest resolution but incurs larger biases. This observation is valid across all losses. While the MagDir-loss could unambiguously provide the best performance for the invariant mass of the \ttbar system, this is not the case for the spin-sensitive variables. For these, no clear pattern that would make one loss preferable emerges; for \chel all losses show a very similar resolution and small differences in the mean deviation, and for \chan the Cartesian-loss has the best performance for the resolution, but also comes with the largest mean deviation.
Generally, the transformer's regression structure employed here yields results that are competitive with the baseline methods. In particular, the resolution of the variables investigated here could be reduced significantly. However, it was found that the regression, which does not provide any probabilistic modelling, unlike $\nu^2$-Flows, incurs much larger biases that also lead to skewness in the distributions.
\begin{table}[H]
    \centering
    \caption{Summaries of the reconstruction metrics for \chel \subrefb{tab:losses:chel_reco} and \chan \subrefb{tab:losses:chan_reco} for the MMT models with different losses, compared to a reconstruction based on $\nu^2$-Flows. The uncertainties represent the statistical variations in the evaluation dataset. }
    \begin{subtable}{1.0\textwidth}
        \centering
        \caption{\textbf{Spin-Sensitive Variable} \chel}
        \label{tab:losses:chel_reco}
        \input{plots/compare_losses/deviations/c_hel_reconstruction_metrics_table.tex}
    \end{subtable}
    \begin{subtable}{1.0\textwidth}
        \centering
        \caption{\textbf{Spin-Sensitive Variable} \chan}
        \label{tab:losses:chan_reco}
        \input{plots/compare_losses/deviations/c_han_reconstruction_metrics_table.tex}
    \end{subtable}
\end{table}%

As before, some kinematic regions are of particular interest and have been shown to be challenging in previous analyses; the effects of certain event variables are investigated. The plots in \cref{fig:full_reco:ttbar_mass_binned_reso_ttbar_mass,fig:full_reco:ttbar_mass_binned_reso_c_hel} show the resolution for the reconstruction using the MMT across different \ttbar-mass bins. For the resolution of the \mttbar itself, an interesting pattern emerges; whereas before the resolution decreased towards lower invariant masses, the opposite is true with the MMT-model. Its performance is mostly constant, with a slight improvement towards low masses. Therefore, the gain in resolution in the lowest-mass bin, which is of interest in many analyses, is approximately a factor of $3$ over the baseline method. This is expected to significantly improve the sensitivity of analyses in this region. For the spin-sensitive variables, \chel in particular, the improvement in resolution offered by the MMT architecture appears to be very consistent. All methods show the lowest performance around $400~\GeV$, which also marks the peak of the \mttbar distribution.


\begin{figure}[H]
    \centering
    \includegraphics[width=0.7\textwidth]{plots/compact_reconstructor/binned_performance/truth_ttbar_mass/binned_resolution_ttbar_mass_truth_ttbar_mass.pdf}
    \caption{Relative Resolution of \mttbar}
    \caption{Relative Resolution of \mttbar computed in bins of the parton-level invariant mass of the \ttbar system.
    Different setups for assignment and regression are compared. The background histogram shows the distribution of events as a function of the number of jets. The uncertainties represent the statistical variations in the evaluation dataset.}
    \label{fig:full_reco:ttbar_mass_binned_reso_ttbar_mass}
\end{figure}%

\begin{figure}[H]
    \centering
    \includegraphics[width=0.7\textwidth]{plots/compact_reconstructor/binned_performance/truth_ttbar_mass/binned_resolution_c_hel_truth_ttbar_mass.pdf}
    \caption{Resolution of \chel}
    \caption{Resolution of \chel computed in bins of the parton-level invariant mass of the \ttbar system. Different setups for assignment and regression are compared. The background histogram shows the distribution of events as a function of the number of jets. The uncertainties represent the statistical variations in the evaluation dataset.}
    \label{fig:full_reco:ttbar_mass_binned_reso_c_hel}
\end{figure}%

Since the mean deviation is also crucial for recovering the shape of a distribution, its behaviour for the invariant mass $m(t\bar{t})$ binned with respect to \mttbar can be found in \cref{fig:full_reco:ttbar_mass_binned_deviation_ttbar_mass}. While it was discussed earlier that the MMT has a strong bias towards lower invariant masses, the plot reveals that the bias's behaviour across the mass range differs markedly between the methods. The reconstruction based on $\nu^2$-Flows yields a mean relative deviation ranging from $-0.1$ to $+0.1$, depending on the mass regime. While its modulus is smaller in all bins, this shows that the difference in the bias is much smaller than observed before, when averaging over the whole spectrum. Thus, it can be said that the methods using $\nu^2$-Flows are also strongly biased in certain regimes, but these biases cancel out when considering the full invariant mass spectrum. For the MMT, on the other hand, the mean relative deviation remains very consistently around $-0.13$. Thus, it might be possible to correct for the bias in the distribution at the reco-level for the MMT. 



\begin{figure}[ht]
    \centering
    \includegraphics[width=0.7\textwidth]{plots/compact_reconstructor/binned_performance/truth_ttbar_mass/binned_deviation_ttbar_mass_truth_ttbar_mass.pdf}
    \caption{Mean Relative Deviation of \mttbar in bins of the \mttbar computed at parton-level. The background histogram shows the distribution of events as a function of the number of jets. The uncertainties represent the statistical variations in the evaluation dataset.}
    \label{fig:full_reco:ttbar_mass_binned_deviation_ttbar_mass}
\end{figure}

The mean deviations of \chel and \chan, as depicted in \cref{fig:full_reco:ttbar_mass_binned_deviation}, show a different picture. It peaks at low masses for all methods. But the methods show a very consistent change in bias across the different $m(t\bar{t})$ regimes.

\subsection{Impact of Gaussian Sampling}
Lastly, the effect of the Gaussian-loss on the variable reconstruction, and their distributions in particular, has to be discussed. As pointed out before, no significant difference between using the mean of the Gaussian-loss-trained MMT and the nominal MMT was found. Therefore, this section compares the Gaussian-loss for reconstructing neutrinos using the mean and random sampling. For the deviation of the neutrinos' momenta, it was found that the Gaussian sampling consistently leads to larger errors. A similar observation can be made for its impact on the resolution of reconstructed variables.
\begin{table}[ht]
    \centering
    \caption{Summary of mean relative deviation and relative resolution for the reconstructed mass of the \ttbar system for the MMT trained with the Gaussian-loss with Mean and Gaussian sampling.}
    \input{plots/gaussian_loss/deviations/ttbar_mass_reconstruction_metrics_table.tex}
    \label{tab:gaussian_loss:ttbar_mass_reso}
    \caption{Summary of \chel-reconstruction performance}
    \input{plots/gaussian_loss/deviations/c_hel_reconstruction_metrics_table.tex}
    \label{tab:gaussian_loss:c_hel_reso}
    \caption{Summary of \chan-reconstruction performance}
    \input{plots/gaussian_loss/deviations/c_han_reconstruction_metrics_table.tex}
    \label{tab:gaussian_loss:c_han_reso}
\end{table}

\Cref{tab:gaussian_loss:ttbar_mass_reso,tab:gaussian_loss:c_hel_reso,tab:gaussian_loss:c_han_reso} provide a summary of the three reconstruction variables considered here. An overall pattern across all the variables can be identified: introducing the Gaussian sampling leads to a decrease in the mean (relative) deviation for all variables, i.e. reduces biases. On the other hand, the (relative) resolution decreases compared with mean-based regression. This loss of resolution varies across variables: while for \mttbar it still exceeds that of the reconstruction with $\nu^2$-Flows, the resolution of the spin-sensitive variables degrades significantly, falling below the performance of $\nu^2$-Flows. The reduction of the bias for the \mttbar is quite substantial with a factor of about $10^{3}$, making the bias smaller than that of $\nu^2$-Flows. For the other variables, such a strong reduction could not be observed, with the reduction being around a factor $2$, and the bias still larger than for reconstruction with $\nu^2$-Flows.
While these metrics summarise the behaviour of the Gaussian-loss on the reconstruction, its impact on their distributions can also be studied. Considering the skew and distortions in the shape discussed earlier, this is one of the main points the Gaussian-loss seeks to address. For the mass of the \ttbar system, plots comparing the distributions at the reco- and truth-levels are shown in \cref{fig:gaussian_loss:ttbar_mass_dist} in \cref{chap:add-plots}. The change in the distributions reflects a less biased reconstruction, eliminating the previously observed peak shift in MMT. It should be noted that while the peak position seems to align better with the truth, its height with respect to the truth level is substantially reduced.
\begin{figure}[ht]
    \centering
    \begin{subfigure}{0.49\textwidth}
        \includegraphics[width=1.0\textwidth]{plots/gaussian_loss/distributions/c_hel_distribution_ratios.pdf}
        \subcaption{\chel}
        \label{fig:gaussian_loss:c_hel_dist}
    \end{subfigure}
    \begin{subfigure}{0.49\textwidth}
        \includegraphics[width=1.0\textwidth]{plots/gaussian_loss/distributions/c_han_distribution_ratios.pdf}
        \subcaption{\chan}
        \label{fig:gaussian_loss:c_han_dist}
    \end{subfigure}
    \caption{Distributions and density ratio between reco-level and parton-level for the MMT with Gaussian-loss for \chel and \chan in \subrefb{fig:gaussian_loss:c_hel_dist} and \subrefb{fig:gaussian_loss:c_han_dist}, respectively.}
    \label{fig:gaussian_loss:spin_variables_dist}
\end{figure}
Comparisons of the distributions of the spin-sensitive variables are shown in \cref{fig:gaussian_loss:spin_variables_dist}, which presents the different reconstruction setups, the truth-level, and the ratios of the corresponding histograms. For both variables, the same behaviour of the reconstruction methods is observed: while the reconstruction with $\nu^2$-Flows provides the most faithful distribution, the MMT provides strong deviations in the tails of the distributions at $\pm1$. These effects are partly mitigated by the Gaussian sampling, which provides a much shallower ratio and a match to the truth-level that is comparable with what is obtained by $\nu^2$-Flows.

While distributions of reconstructed masses of the intermediate particles, i.e. $W$ boson and top quark, are less interesting for the aforementioned analyses, they can be considered a measure for the physical plausibility of the reconstructed neutrino momenta. For these two variables, two things are observed: the reconstructed masses of the MMT are much more spread out, and their peaks are significantly displaced with respect to the nominal values and parton-level distributions. The corresponding distributions can be found in \cref{fig:gaussian_loss:top_mass,fig:gaussian_loss:W_mass} in \cref{app:neutrino_probs}. Notably, the random sampling of the Gaussian-Loss seems to, at least partly, correct the shift in the distributions' peaks. This comes with the limitation of the peaks being even further spread out. 
Even though the mass constraints are not explicitly enforced in $\nu^2$-Flows, the model seems to capture the masses, the $W$-boson mass in particular, very well; despite utilising a random sampling.

To investigate what drives the distortion of the distributions, several variables related to the angles between the reconstructed objects were examined to identify the cause of the strong deviations and the shape produced by the MMT regression. While the underlying structure remains largely elusive, the recovery of the shape by Gaussian sampling indicates that a major factor is that the reconstructed variables are often highly non-linear with respect to the neutrinos' momenta. Therefore, their computation does not commute with taking the mean of the variable over the possible neutrino momenta configurations, i.e.
\begin{align}
    \langle m(\ell^-,\ell^+,b,\bar{b},\nu,\bar{\nu})\rangle_{\nu,\bar{\nu}}\neq m(\ell^-,\ell^+,b,\bar{b},\langle\nu\rangle,\langle\bar{\nu}\rangle)~.
\end{align}
Note that in this notation, the particles denote their four-momenta.
The same argument holds for the computation of the spin-sensitive variables. This can lead to significant biases in the distribution by collapsing the neutrino momenta to their mean. Especially if the distribution has inherent multimodality, which, given the quadratic form of the mass-constraint (see \cref{sec:kinematic_constraints}), is expected, a collapse of the distribution to the mean can lead to significant deviations. Note that a possible multimodality in the conditional probability cannot be properly recovered by the Gaussian sampling here, as it lacks the expressivity to model such a distribution.
\begin{figure}[H]
    \centering
    \includegraphics[width=0.7\textwidth]{plots/gaussian_loss_plots/gaussian_loss_mttbar_posterior_deviation.pdf}
    \caption{Relative deviation between the reconstructed and true $m(t\bar{t})$ of the MMT trained with a Gaussian-Loss. The different curves show whether the invariant mass was computed using the mean momentum or by propagating the neutrino probabilities and averaging the obtained masses.}
    \label{fig:gaussian_loss:mttbar_posterior_deviation}
\end{figure}
Plots showing the impact of propagating the posterior for the case of the Gaussian conditional probability density for the neutrino momenta are shown in \cref{app:neutrino_probs} for the variables discussed here. Due to the inaccessibility of the probability density of $\nu^2$-Flow, a comparison of the methods' probabilities was not possible. It was found that by propagating the obtained conditional probability distribution to the reconstructed variables and averaging over the variable's distribution, one can obtain much lower biases and improved resolution. This can be seen in \cref{fig:gaussian_loss:mttbar_posterior_deviation}. While random sampling reduces bias at the expense of resolution, this was not observed in this reconstruction strategy. Instead, both metrics improve when the probability distribution is propagated to the reconstructed variable. 

Similar plots for the spin-sensitive variables can be found in \cref{fig:gaussian_loss:c_hel_posterior_deviation,fig:gaussian_loss:c_han_posterior_deviation} in \cref{app:neutrino_probs}. While this provides a promising approach to enhancing neutrino regression performance, it was not investigated further in this work due to time constraints.
In conclusion, the methods presented in this thesis can provide significant improvements in the reconstruction of variables critical to analyses. Generally, the evaluation depends very strongly on the variable and metrics one employs. Therefore, performance assessment is highly degenerate and warrants further investigation in future analyses.
